server {
    if ($host = dialogue.datingproject.net) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


        listen 80 default_server;
        listen [::]:80 default_server;

        server_name dialogue.datingproject.net;
    return 404; # managed by Certbot


}