const state = {
    connections: [],
}

const getters = {
    connections: state => state.connections
}

const actions = {

}

const mutations = {

}

export default { state, getters, actions, mutations }